using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class cs_vec : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Vector3 v;
        v = new Vector3(1.0f, 2.0f, 3.0f);

        Debug.Log(v);
    }

    // Update is called once per frame
    void Update()
    {

    }
}
